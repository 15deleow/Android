package com.example.flightsearch.data

import androidx.datastore.core.DataStore
import java.util.prefs.Preferences

class FlightSearchRepository(private val dataStore: DataStore<Preferences>) {
    companion object{

    }
}